<Window x:Class="GTAVInjector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="GTA GG'S Launcher" 
        Height="700" Width="1000"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- Animación de parallax mejorada -->
        <Storyboard x:Key="ParallaxAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="ParallaxLayer1"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="0" To="-200" Duration="0:0:25" AutoReverse="True">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ParallaxLayer2"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="0" To="-300" Duration="0:0:35" AutoReverse="True">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource ParallaxAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border BorderBrush="#FF9EFF" BorderThickness="2" CornerRadius="15" Padding="0">
        <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <!-- Fondo animado con parallax -->
            <Canvas ClipToBounds="True">
                <Canvas.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#13001F" Offset="0"/>
                        <GradientStop Color="#1D022D" Offset="0.6"/>
                        <GradientStop Color="#13001F" Offset="1"/>
                    </LinearGradientBrush>
                </Canvas.Background>
                
                <!-- Capa de parallax 1 -->
                <Canvas x:Name="ParallaxLayer1" Width="1400" Height="700" Opacity="0.8">
                    <Canvas.RenderTransform>
                        <TranslateTransform X="0" Y="0"/>
                    </Canvas.RenderTransform>
                    <Ellipse Width="300" Height="300" Canvas.Left="50" Canvas.Top="50" Fill="#7B3FD1" Opacity="0.7">
                        <Ellipse.Effect>
                            <BlurEffect Radius="5"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="200" Height="200" Canvas.Left="600" Canvas.Top="300" Fill="#A946FF" Opacity="0.8">
                        <Ellipse.Effect>
                            <BlurEffect Radius="3"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="150" Height="150" Canvas.Left="350" Canvas.Top="150" Fill="#BB4CFF" Opacity="0.6">
                        <Ellipse.Effect>
                            <BlurEffect Radius="2"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="100" Height="100" Canvas.Left="800" Canvas.Top="80" Fill="#D872FF" Opacity="0.9">
                        <Ellipse.Effect>
                            <BlurEffect Radius="1"/>
                        </Ellipse.Effect>
                    </Ellipse>
                </Canvas>
                
                <!-- Capa de parallax 2 -->
                <Canvas x:Name="ParallaxLayer2" Width="1400" Height="700" Opacity="0.7">
                    <Canvas.RenderTransform>
                        <TranslateTransform X="0" Y="0"/>
                    </Canvas.RenderTransform>
                    <Ellipse Width="250" Height="250" Canvas.Left="450" Canvas.Top="40" Fill="#FF72E2" Opacity="0.6">
                        <Ellipse.Effect>
                            <BlurEffect Radius="4"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="180" Height="180" Canvas.Left="150" Canvas.Top="350" Fill="#A946FF" Opacity="0.7">
                        <Ellipse.Effect>
                            <BlurEffect Radius="3"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="120" Height="120" Canvas.Left="750" Canvas.Top="200" Fill="#7B3FD1" Opacity="0.8">
                        <Ellipse.Effect>
                            <BlurEffect Radius="2"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="80" Height="80" Canvas.Left="900" Canvas.Top="320" Fill="#D872FF" Opacity="0.9">
                        <Ellipse.Effect>
                            <BlurEffect Radius="1"/>
                        </Ellipse.Effect>
                    </Ellipse>
                </Canvas>
            </Canvas>
            
            <!-- Contenido principal -->
            <Grid Margin="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                
                <!-- Barra de título personalizada -->
                <Border Grid.Row="0" Background="#1A0128" CornerRadius="13,13,0,0" Margin="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Logo y título -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="15,0">
                        <Image x:Name="LogoImage" Width="40" Height="40" Margin="0,0,10,0" 
                               Source="pack://application:,,,/Resources/logo tessio.png" VerticalAlignment="Center"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="TessioScript Inyector" FontSize="18" FontWeight="Bold" Foreground="#FF9EFF"/>
                            <TextBlock x:Name="VersionText" Text="v2.0.0" FontSize="10" Foreground="#D3B4FF"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Selector de idioma -->
                    <Border Grid.Column="1" Margin="0,0,20,0" HorizontalAlignment="Right"
                            Background="#2A0B3A" CornerRadius="20" Padding="12,6">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                       Foreground="#FF9EFF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <StackPanel>
                                <TextBlock x:Name="LanguageLabel" Text="Idioma" FontSize="11" Foreground="#D3B4FF"/>
                                <ComboBox x:Name="LanguageSelector" Width="160" Height="30"
                                          Background="#2A0B3A" Foreground="#FFFFFF" BorderBrush="#A946FF"
                                          Padding="4,0" FontWeight="SemiBold" BorderThickness="1"
                                          FocusVisualStyle="{x:Null}"
                                          SelectionChanged="LanguageSelector_SelectionChanged">
                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox">
                                            <Setter Property="Background" Value="#2A0B3A"/>
                                            <Setter Property="BorderBrush" Value="#A946FF"/>
                                            <Setter Property="Foreground" Value="#FFFFFF"/>
                                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ComboBox">
                                                        <Border x:Name="MainBorder"
                                                                Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="4"
                                                                MouseLeftButtonDown="ComboBox_MouseDown">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="20"/>
                                                                </Grid.ColumnDefinitions>
                                                                
                                                                <ContentPresenter Grid.Column="0"
                                                                                x:Name="ContentSite"
                                                                                Content="{TemplateBinding SelectionBoxItem}"
                                                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                                IsHitTestVisible="False"
                                                                                Margin="{TemplateBinding Padding}"
                                                                                VerticalAlignment="Center"
                                                                                HorizontalAlignment="Left"
                                                                                TextElement.Foreground="{TemplateBinding Foreground}"/>
                                                                
                                                                <ToggleButton Grid.Column="1"
                                                                            x:Name="ToggleButton"
                                                                            Background="Transparent"
                                                                            BorderBrush="Transparent"
                                                                            BorderThickness="0"
                                                                            Focusable="False"
                                                                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                            ClickMode="Press">
                                                                    <ToggleButton.Style>
                                                                        <Style TargetType="ToggleButton">
                                                                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                                                                            <Setter Property="Template">
                                                                                <Setter.Value>
                                                                                    <ControlTemplate TargetType="ToggleButton">
                                                                                        <Border x:Name="ToggleBorder" Background="Transparent">
                                                                                            <Path Fill="#FFFFFF"
                                                                                                  Data="M0,0 L4,4 L8,0 Z"
                                                                                                  HorizontalAlignment="Center"
                                                                                                  VerticalAlignment="Center"/>
                                                                                        </Border>
                                                                                        <ControlTemplate.Triggers>
                                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                                <Setter TargetName="ToggleBorder" Property="Background" Value="Transparent"/>
                                                                                            </Trigger>
                                                                                        </ControlTemplate.Triggers>
                                                                                    </ControlTemplate>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </Style>
                                                                    </ToggleButton.Style>
                                                                </ToggleButton>
                                                                
                                                                <Popup x:Name="Popup"
                                                                     Placement="Bottom"
                                                                     IsOpen="{TemplateBinding IsDropDownOpen}"
                                                                     AllowsTransparency="True"
                                                                     Focusable="False"
                                                                     PopupAnimation="Slide">
                                                                    <Grid MinWidth="{TemplateBinding ActualWidth}">
                                                                        <Border Background="#3B0F4D"
                                                                              BorderBrush="#FF9EFF"
                                                                              BorderThickness="1"
                                                                              CornerRadius="4"
                                                                              Padding="0,4">
                                                                            <ScrollViewer x:Name="DropDownScrollViewer">
                                                                                <StackPanel IsItemsHost="True"/>
                                                                            </ScrollViewer>
                                                                        </Border>
                                                                    </Grid>
                                                                </Popup>
                                                            </Grid>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="MainBorder" Property="Background" Value="#3B0F4D"/>
                                                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#FF9EFF"/>
                                                            </Trigger>
                                                            <Trigger Property="IsFocused" Value="True">
                                                                <Setter TargetName="MainBorder" Property="Background" Value="#3B0F4D"/>
                                                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#FF9EFF"/>
                                                            </Trigger>
                                                            <Trigger Property="IsDropDownOpen" Value="True">
                                                                <Setter TargetName="MainBorder" Property="Background" Value="#3B0F4D"/>
                                                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#FF9EFF"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ComboBox.Style>
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="#FFFFFF"/>
                                            <Setter Property="Padding" Value="8,4"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ComboBoxItem">
                                                        <Border x:Name="ItemBorder"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Left"
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter TargetName="ItemBorder" Property="Background" Value="#A946FF"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="ItemBorder" Property="Background" Value="#4D1A5E"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ItemBorder" Property="Background" Value="#A946FF"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                    <ComboBoxItem Content="🇺🇸 English" Tag="en"/>
                                    <ComboBoxItem Content="🇪🇸 Español" Tag="es"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Botones de ventana -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="─" Style="{StaticResource WindowButton}" Click="Minimize_Click"/>
                        <Button Content="✕" Style="{StaticResource CloseButton}" Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

    <!-- Contenido principal -->
                <Grid Grid.Row="1" Margin="0" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Panel izquierdo - Lista de DLLs -->
                    <Border Grid.Column="0" Background="#1F012F99" CornerRadius="10" Padding="18">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Título y botones -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock x:Name="DllListTitle" Text="DLL List" FontSize="16" FontWeight="Bold" 
                                          Foreground="#FFD9FF" VerticalAlignment="Center"/>
                                <Button x:Name="AddDllButton" Content="+ Add DLL" Margin="15,0,0,0" 
                                       Style="{StaticResource PrimaryButton}" Click="AddDll_Click"/>
                            </StackPanel>
                            
                            <!-- Auto-inyector -->
                            <CheckBox x:Name="AutoInjectCheckbox" Grid.Row="1" Margin="0,0,0,10"
                                     Content="Auto-Inject on game start" Foreground="#FFFFFF"
                                     Checked="AutoInject_Changed" Unchecked="AutoInject_Changed"/>

                            <!-- Lista de DLLs con ScrollViewer -->
                            <ScrollViewer Grid.Row="2" Margin="0,0,0,10" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <ListView x:Name="DllListView" Background="#15011E" 
                                         BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListViewItem">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderThickness="0"
                                                                Margin="2,1">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="90"/>
                                                <ColumnDefinition Width="70"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <CheckBox Grid.Column="0" IsChecked="{Binding Enabled}" 
                                                     VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding FileName}" 
                                                      Foreground="#FFFFFF" VerticalAlignment="Center" Margin="10,0"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Status}" 
                                                      VerticalAlignment="Center" HorizontalAlignment="Center"
                                                      FontSize="11" TextWrapping="Wrap">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Injected">
                                                                <Setter Property="Foreground" Value="#BB4CFF"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Not Injected">
                                                                <Setter Property="Foreground" Value="#FF4FD8"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <Button Grid.Column="3" x:Name="RemoveButton"
                                                   Style="{StaticResource DangerButton}"
                                                   Click="RemoveDll_Click" Tag="{Binding}"
                                                   FontSize="10" Padding="8,4">
                                                <Button.Content>
                                                    <TextBlock x:Name="RemoveButtonText" Text="Quitar"/>
                                                </Button.Content>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            </ScrollViewer>
                            
                            <!-- Botones de acción -->
                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button x:Name="LaunchButton" Content="Launch Game" Width="120" Margin="5"
                                       Style="{StaticResource SuccessButton}" Click="LaunchGame_Click"/>
                                <Button x:Name="InjectButton" Content="Inject DLLs" Width="120" Margin="5"
                                       Style="{StaticResource PrimaryButton}" Click="InjectDlls_Click"/>
                                <Button x:Name="KillButton" Content="Kill Game" Width="120" Margin="5"
                                       Style="{StaticResource DangerButton}" Click="KillGame_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- Panel derecho - Configuración -->
                    <Border Grid.Column="2" Background="#1F012F99" CornerRadius="10" Padding="18">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel>
                                <!-- Estatus de versión -->
                                <Border Background="#15011E" CornerRadius="8" Padding="12" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock x:Name="VersionStatusTitle" Text="Version Status" 
                                                  FontWeight="Bold" Foreground="#FFD9FF" Margin="0,0,0,5"/>
                                        <TextBlock x:Name="VersionStatusText" Text="Checking..." 
                                                  Foreground="#D3B4FF" FontSize="12" Margin="0,0,0,5"/>
                                        <Button x:Name="UpdateButton" Content="Update Available" 
                                               Style="{StaticResource WarningButton}" 
                                               Visibility="Collapsed" Click="Update_Click"/>
                                        <Button x:Name="ChangelogButton" Content="View Changelog" 
                                               Style="{StaticResource PrimaryButton}" 
                                               Visibility="Collapsed" Click="Changelog_Click" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Tipo de juego -->
                                <TextBlock x:Name="GameTypeTitle" Text="Game Type" FontWeight="Bold" 
                                          Foreground="#FFD9FF" Margin="0,0,0,5"/>
                                <RadioButton x:Name="LegacyRadio" Content="Legacy (GTA5.exe)" 
                                            Foreground="#FFFFFF" Margin="0,5" GroupName="GameType" 
                                            Checked="GameType_Changed"/>
                                <RadioButton x:Name="EnhancedRadio" Content="Enhanced (GTA5_Enhanced.exe)" 
                                            Foreground="#FFFFFF" Margin="0,5" GroupName="GameType" 
                                            Checked="GameType_Changed"/>
                                
                                <Separator Margin="0,15" Background="#A946FF"/>
                                
                                <!-- Launcher -->
                                <TextBlock x:Name="LauncherTitle" Text="Launcher" FontWeight="Bold" 
                                          Foreground="#FFD9FF" Margin="0,0,0,5"/>
                                <RadioButton x:Name="RockstarRadio" Content="Rockstar Games" 
                                            Foreground="#FFFFFF" Margin="0,5" GroupName="Launcher" 
                                            Checked="Launcher_Changed"/>
                                <RadioButton x:Name="EpicRadio" Content="Epic Games" 
                                            Foreground="#FFFFFF" Margin="0,5" GroupName="Launcher" 
                                            Checked="Launcher_Changed"/>
                                <RadioButton x:Name="SteamRadio" Content="Steam" 
                                            Foreground="#FFFFFF" Margin="0,5" GroupName="Launcher" 
                                            Checked="Launcher_Changed"/>

                                <Separator Margin="0,15" Background="#A946FF"/>

                                <!-- Redes Sociales -->
                                <TextBlock x:Name="SocialMediaTitle" Text="Social Media" FontWeight="Bold" 
                                          Foreground="#FFD9FF" Margin="0,0,0,8"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                                    <Button x:Name="DiscordButton" Margin="5" Width="50" Height="50" 
                                           Style="{StaticResource SocialButton}" Click="Discord_Click"
                                           ToolTip="Únete a nuestro Discord">
                                        <Image Source="pack://application:,,,/Resources/social/discord.png" Stretch="Uniform"/>
                                    </Button>
                                    <Button x:Name="TikTokButton" Margin="5" Width="50" Height="50" 
                                           Style="{StaticResource SocialButton}" Click="TikTok_Click"
                                           ToolTip="Síguenos en TikTok">
                                        <Image Source="pack://application:,,,/Resources/social/tiktok.png" Stretch="Uniform"/>
                                    </Button>
                                    <Button x:Name="TwitchButton" Margin="5" Width="50" Height="50" 
                                           Style="{StaticResource SocialButton}" Click="Twitch_Click"
                                           ToolTip="Síguenos en Twitch">
                                        <Image Source="pack://application:,,,/Resources/social/twitch.png" Stretch="Uniform"/>
                                    </Button>
                                    <Button x:Name="YouTubeButton" Margin="5" Width="50" Height="50" 
                                           Style="{StaticResource SocialButton}" Click="YouTube_Click"
                                           ToolTip="Síguenos en YouTube">
                                        <Image Source="pack://application:,,,/Resources/social/youtube.png" Stretch="Uniform"/>
                                    </Button>
                                </StackPanel>

                                <Separator Margin="0,0,0,15" Background="#A946FF"/>

                                <!-- Requisitos -->
                                <TextBlock x:Name="RequirementsTitle" Text="Requirements" FontWeight="Bold" 
                                          Foreground="#FFD9FF" Margin="0,0,0,5"/>
                                <Border Background="#15011E" CornerRadius="8" Padding="12">
                                    <StackPanel>
                                        <TextBlock Text="• Windows 10/11 (64-bit)" Foreground="#D3B4FF" FontSize="11" Margin="0,2"/>
                                        <TextBlock Text="• .NET 8.0 Runtime" Foreground="#D3B4FF" FontSize="11" Margin="0,2"/>
                                        <TextBlock x:Name="VcRequirementText" Text="• VC++ Redist x86 &amp; x64" Foreground="#D3B4FF" FontSize="11" Margin="0,2"/>
                                        <TextBlock x:Name="GtaRequirementText" Text="• GTA V installed" Foreground="#D3B4FF" FontSize="11" Margin="0,2"/>
                                        <TextBlock x:Name="AdminRequirementText" Text="• Administrator rights" Foreground="#D3B4FF" FontSize="11" Margin="0,2"/>
                                    </StackPanel>
                                </Border>
                            
                                <!-- Imagen destacada -->
                                <Border Background="#15011E" CornerRadius="10" Padding="10" Margin="0,15,0,0">
                                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly" MaxHeight="150">
                                        <Image Source="pack://application:,,,/Resources/logo tessio.png" Height="150" Width="150" Stretch="Uniform"
                                               RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                    </Viewbox>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
                
                <!-- Barra de estado -->
                <Border Grid.Row="2" Background="#15011E" CornerRadius="0,0,13,13" Padding="12,6" Margin="0">
                    <Grid>
                        <TextBlock x:Name="StatusText" Text="Listo" Foreground="#FF9EFF" 
                                  VerticalAlignment="Center"/>
                        <TextBlock x:Name="GameStatusText" Text="Game: Not Running" 
                                  Foreground="#FF4FD8" HorizontalAlignment="Right" 
                                  VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
